import React from 'react';
import { storiesOf } from '@storybook/react';
import InputFormControl from 'form-controls/InputFormControl';

storiesOf('InputFormControl', module).add('default', () => (
	<div>
		<InputFormControl />
	</div>
));
